import { CustomHighLight2Directive } from './custom-high-light2.directive';

describe('CustomHighLight2Directive', () => {
  it('should create an instance', () => {
    const directive = new CustomHighLight2Directive();
    expect(directive).toBeTruthy();
  });
});
