import { CustomHighLight1Directive } from './custom-high-light1.directive';

describe('CustomHighLight1Directive', () => {
  it('should create an instance', () => {
    const directive = new CustomHighLight1Directive();
    expect(directive).toBeTruthy();
  });
});
