import { CustomHighLight3Directive } from './custom-high-light3.directive';

describe('CustomHighLight3Directive', () => {
  it('should create an instance', () => {
    const directive = new CustomHighLight3Directive();
    expect(directive).toBeTruthy();
  });
});
